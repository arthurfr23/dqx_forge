## What this changes

<!-- One or two sentences. Link the issue if there is one. -->

## How it was verified

<!--
`npm run typecheck` is the floor, not the bar. For anything touching bundle
generation, a Python task or a workspace call, say what you actually ran:
deployed a bundle, executed the job, imported a contract.
-->

- [ ] `npm run typecheck` passes
- [ ] Verified against a real Databricks workspace (describe how)
- [ ] `CHANGELOG.md` updated under `## [Unreleased]`
- [ ] README updated, if documented behaviour changed
- [ ] New user-facing strings go through `i18n/messages.ts` in all three languages
