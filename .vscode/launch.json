{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Rodar DQX Forge",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}"],
      "outFiles": ["${workspaceFolder}/dist/**/*.js"],
      "preLaunchTask": "compilar extensão"
    }
  ]
}
